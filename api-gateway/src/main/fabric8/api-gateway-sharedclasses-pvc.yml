#apiVersion: v1
#kind: PersistentVolumeClaim
#metadata:
#  name: ${project.artifactId}-sharedclasses
#  labels:
#    app: api-gateway
#    group: com.github.morganyvm
#    version: ${project.artifactId}
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 100Mi